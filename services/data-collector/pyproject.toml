[project]
name = "data-collector"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.67",
    "grpcio-tools>=1.67",
    "httpx>=0.28",
    "redis[hiredis]>=5.2",
    "pydantic-settings>=2.6",
    "structlog>=24.0",
]
